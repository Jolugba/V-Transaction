<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.VTransaction" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VTransaction" parent="Base.Theme.VTransaction" />
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenBackground">@color/purple</item>
        <item name="postSplashScreenTheme">@style/Theme.VTransaction</item>
    </style>
</resources>